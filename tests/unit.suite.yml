# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Redis:
            host: '127.0.0.1'
            port: 6379
            database: 0
            cleanupBefore: 'test'